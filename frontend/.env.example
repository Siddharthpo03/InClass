# =============================================================================
# InClass Frontend - Environment Variables Template (P3 - No real values)
# =============================================================================
# Copy to .env and set values locally. NEVER commit .env to git.
#   cp .env.example .env
# Vite exposes only variables prefixed with VITE_
# =============================================================================

# API base URL
VITE_API_BASE_URL=https://api.inclass.siddharthp.com/api

# Socket.io URL
VITE_SOCKET_URL=https://api.inclass.siddharthp.com
